import { Head, Link, useForm } from '@inertiajs/react';
import { KeyRound, LockKeyhole, ShieldCheck } from 'lucide-react';
import type { FormEvent } from 'react';

export default function ResetPassword({ token, email }: { token: string; email: string }) { const form = useForm({ token, email, password: '', password_confirmation: '' }); function submit(e: FormEvent) { e.preventDefault(); form.post('/reset-password'); } return <><Head title="Reset Password" /><main className="flex min-h-screen items-center justify-center bg-gradient-to-br from-slate-950 via-[#092b36] to-teal-900 p-5"><section className="w-full max-w-md overflow-hidden rounded-3xl bg-white shadow-2xl shadow-black/30"><div className="bg-gradient-to-br from-slate-900 to-teal-900 p-7 text-white"><div className="flex size-12 items-center justify-center rounded-2xl bg-teal-300/15 text-teal-200"><KeyRound size={22} /></div><h1 className="mt-5 text-2xl font-semibold">Buat password baru.</h1><p className="mt-2 text-sm text-slate-300">Gunakan password unik yang belum pernah dipakai.</p></div><form onSubmit={submit} className="space-y-4 p-7"><Field label="Email" error={form.errors.email}><input type="email" className="input" value={form.data.email} onChange={(e) => form.setData('email', e.target.value)} /></Field><Field label="Password baru" error={form.errors.password}><div className="relative"><LockKeyhole size={16} className="absolute left-3 top-1/2 -translate-y-1/2 text-slate-400" /><input autoFocus type="password" className="input pl-10" value={form.data.password} onChange={(e) => form.setData('password', e.target.value)} /></div></Field><Field label="Konfirmasi password"><input type="password" className="input" value={form.data.password_confirmation} onChange={(e) => form.setData('password_confirmation', e.target.value)} /></Field><button disabled={form.processing} className="w-full rounded-xl bg-gradient-to-r from-teal-600 to-cyan-600 py-3 text-sm font-semibold text-white disabled:opacity-50">{form.processing ? 'Memperbarui...' : 'Perbarui password'}</button><p className="flex items-center justify-center gap-1 text-[10px] text-slate-400"><ShieldCheck size={12} /> Token hanya dapat digunakan satu kali.</p><Link href="/login" className="block text-center text-xs font-semibold text-teal-700">Kembali ke login</Link></form></section></main></>; }
function Field({ label, error, children }: { label: string; error?: string; children: React.ReactNode }) { return <label className="block"><span className="text-xs font-semibold text-slate-700">{label}</span><div className="mt-2">{children}</div>{error && <p className="mt-1 text-xs text-rose-600">{error}</p>}</label>; }
