import { Head, Link, useForm } from '@inertiajs/react';
import { ArrowLeft, KeyRound, Mail, ShieldCheck } from 'lucide-react';
import type { FormEvent } from 'react';

export default function ForgotPassword() { const form = useForm({ email: '' }); function submit(e: FormEvent) { e.preventDefault(); form.post('/forgot-password'); } return <><Head title="Lupa Password" /><main className="flex min-h-screen items-center justify-center bg-gradient-to-br from-slate-950 via-[#092b36] to-teal-900 p-5"><section className="w-full max-w-md overflow-hidden rounded-3xl bg-white shadow-2xl shadow-black/30"><div className="bg-gradient-to-br from-slate-900 to-teal-900 p-7 text-white"><div className="flex size-12 items-center justify-center rounded-2xl bg-teal-300/15 text-teal-200 ring-1 ring-teal-200/20"><KeyRound size={22} /></div><h1 className="mt-5 text-2xl font-semibold">Pulihkan akses akun.</h1><p className="mt-2 text-sm leading-6 text-slate-300">Kami akan mengirim tautan reset jika email terdaftar.</p></div><form onSubmit={submit} className="space-y-5 p-7"><label className="block"><span className="text-xs font-semibold text-slate-700">Email akun</span><div className="relative mt-2"><Mail size={16} className="absolute left-3 top-1/2 -translate-y-1/2 text-slate-400" /><input autoFocus type="email" className="input pl-10" value={form.data.email} onChange={(e) => form.setData('email', e.target.value)} /></div>{form.errors.email && <p className="mt-1 text-xs text-rose-600">{form.errors.email}</p>}</label><button disabled={form.processing} className="w-full rounded-xl bg-gradient-to-r from-teal-600 to-cyan-600 py-3 text-sm font-semibold text-white shadow-lg shadow-teal-600/20 disabled:opacity-50">{form.processing ? 'Mengirim...' : 'Kirim tautan pemulihan'}</button><div className="flex items-center justify-between text-xs"><Link href="/login" className="flex items-center gap-1 font-semibold text-teal-700"><ArrowLeft size={13} /> Kembali masuk</Link><span className="flex items-center gap-1 text-slate-400"><ShieldCheck size={13} /> Aman</span></div></form></section></main></>; }
