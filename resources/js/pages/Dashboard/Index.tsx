import { Head } from '@inertiajs/react';
import { ArrowDownRight, ArrowUpRight, CalendarDays, CheckCircle2, Clock3, MoreHorizontal, TrendingUp, WalletCards } from 'lucide-react';
import { DashboardLayout } from '@/layouts/DashboardLayout';
import type { Stat } from '@/types';

type Props = { role: string; stats: Stat[]; term: { name: string; semester: string } | null; recentPayments: Array<{ external_reference: string; amount: string | number; status: string; paid_at: string | null }>; system: { users: number; database: string; bank: string } };

const tone: Record<Stat['tone'], string> = { blue: 'bg-blue-50 text-blue-600', amber: 'bg-amber-50 text-amber-600', violet: 'bg-violet-50 text-violet-600', emerald: 'bg-emerald-50 text-emerald-600' };

export default function Dashboard({ role, stats, term, recentPayments, system }: Props) {
  return <DashboardLayout><Head title="Dashboard" /><section className="flex flex-col justify-between gap-5 md:flex-row md:items-end"><div><p className="text-sm font-medium text-teal-600">Selamat pagi, ruang kendali</p><h1 className="mt-2 text-3xl font-semibold tracking-[-.04em] text-slate-950 lg:text-4xl">Ringkasan operasional.</h1><p className="mt-2 text-sm text-slate-500">Pantau apa yang perlu bergerak hari ini sebagai <span className="font-semibold text-slate-700">{role}</span>.</p></div><div className="flex items-center gap-2 rounded-xl bg-white px-4 py-3 text-sm ring-1 ring-slate-200"><CalendarDays size={16} className="text-slate-400" />{term?.name ?? 'Belum ada periode aktif'}<ChevronDownIcon /></div></section><div className="mt-8 grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{stats.map((item) => <article key={item.label} className="group rounded-2xl bg-white p-5 ring-1 ring-slate-200/80 transition hover:-translate-y-0.5 hover:shadow-xl hover:shadow-slate-200/50"><div className="flex items-start justify-between"><div className={`flex size-10 items-center justify-center rounded-xl ${tone[item.tone]}`}><TrendingUp size={18} /></div><button className="text-slate-300 hover:text-slate-700"><MoreHorizontal size={18} /></button></div><p className="mt-6 text-xs font-medium text-slate-500">{item.label}</p><p className="mt-1 text-2xl font-semibold tracking-tight text-slate-950">{item.value}</p><p className="mt-2 flex items-center gap-1 text-xs text-slate-400"><ArrowUpRight size={13} className="text-emerald-500" />{item.detail}</p></article>)}</div><div className="mt-6 grid gap-6 xl:grid-cols-[1.35fr_.65fr]"><section className="rounded-2xl bg-white p-6 ring-1 ring-slate-200/80"><div className="flex items-center justify-between"><div><h2 className="font-semibold text-slate-950">Aktivitas pembayaran</h2><p className="mt-1 text-xs text-slate-400">Transaksi terbaru dari kanal pembayaran terverifikasi</p></div><button className="rounded-lg p-2 text-slate-400 hover:bg-slate-50"><MoreHorizontal size={18} /></button></div><div className="mt-7 flex h-44 items-end gap-2 border-b border-slate-100 px-2 pb-0">{[38, 56, 44, 68, 53, 82, 74, 92, 64, 78, 90, 86, 100, 88, 96, 82, 92, 100].map((height, index) => <div key={index} className={`group/bar relative flex-1 rounded-t-md ${index > 14 ? 'bg-teal-300' : 'bg-slate-100'} transition hover:bg-teal-200`} style={{ height: `${height}%` }}><span className="absolute -top-7 left-1/2 hidden -translate-x-1/2 rounded bg-slate-900 px-1.5 py-1 text-[10px] text-white group-hover/bar:block">{height}k</span></div>)}</div><div className="mt-4 flex justify-between text-[10px] text-slate-400"><span>01 Jul</span><span>08 Jul</span><span>15 Jul</span><span>Hari ini</span></div></section><section className="rounded-2xl bg-slate-900 p-6 text-white shadow-xl shadow-slate-900/10"><div className="flex items-start justify-between"><div><p className="text-xs text-slate-400">Status sistem</p><h2 className="mt-1 font-semibold">Semua layanan normal</h2></div><span className="flex size-9 items-center justify-center rounded-xl bg-emerald-400/10 text-emerald-300"><CheckCircle2 size={19} /></span></div><div className="mt-8 space-y-4">{[['Database akademik', system.database], ['Akun aktif', `${system.users} akun`], ['Integrasi BSI VA', system.bank]].map(([name, value]) => <div key={name} className="flex items-center justify-between border-b border-white/10 pb-3 text-sm"><span className="text-slate-400">{name}</span><span className="flex items-center gap-2 text-xs text-slate-200"><span className="size-1.5 rounded-full bg-emerald-300" />{value}</span></div>)}</div><div className="mt-8 rounded-xl bg-white/5 p-3 text-xs leading-5 text-slate-400"><Clock3 size={14} className="mb-2 text-teal-300" />Sinkronisasi terakhir beberapa detik lalu.</div></section></div><section className="mt-6 overflow-hidden rounded-2xl bg-white ring-1 ring-slate-200/80"><div className="flex items-center justify-between border-b border-slate-100 p-6"><div><h2 className="font-semibold text-slate-950">Transaksi terakhir</h2><p className="mt-1 text-xs text-slate-400">Audit pembayaran dan status alokasi</p></div><a href="#" className="text-xs font-semibold text-teal-600">Lihat semua</a></div>{recentPayments.length ? <div className="overflow-x-auto"><table className="w-full text-left text-sm"><thead className="bg-slate-50 text-xs text-slate-400"><tr><th className="px-6 py-3 font-medium">Referensi</th><th className="px-6 py-3 font-medium">Tanggal</th><th className="px-6 py-3 font-medium">Nominal</th><th className="px-6 py-3 font-medium">Status</th></tr></thead><tbody className="divide-y divide-slate-100">{recentPayments.map((payment) => <tr key={payment.external_reference}><td className="px-6 py-4 font-medium text-slate-700">{payment.external_reference}</td><td className="px-6 py-4 text-slate-500">{payment.paid_at ? new Date(payment.paid_at).toLocaleDateString('id-ID') : '—'}</td><td className="px-6 py-4 font-semibold text-slate-800">Rp {Number(payment.amount).toLocaleString('id-ID')}</td><td className="px-6 py-4"><span className="rounded-full bg-emerald-50 px-2.5 py-1 text-xs font-medium text-emerald-600">{payment.status}</span></td></tr>)}</tbody></table></div> : <div className="flex flex-col items-center justify-center px-6 py-12 text-center"><WalletCards size={28} className="text-slate-300" /><p className="mt-3 text-sm font-medium text-slate-600">Belum ada transaksi</p><p className="mt-1 text-xs text-slate-400">Data akan muncul setelah callback pembayaran diproses.</p></div>}</section></DashboardLayout>;
}

function ChevronDownIcon() { return <ArrowDownRight size={15} className="ml-1 rotate-45 text-slate-400" />; }
